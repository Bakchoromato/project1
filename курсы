#include <iostream>
using namespace std;

int main()
{
    
    int number;
    cin >> number;
    
    if(number > 50)
    {
        number = number - 20;
    } else if(number < 50)
    {
        number = number + 20;
    }
    
    cout << "result: " << number << endl;
    return 0;
}
